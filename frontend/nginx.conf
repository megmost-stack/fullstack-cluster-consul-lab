server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Wichtig für Single Page Apps (React/Vue), falls vorhanden:
        try_files $uri $uri/ /index.html;
    }
}
